1. Go inside client dir and run npm update.
   1. Then open terminal inside client dir and run npm run dev.
2. Go inside server dir and run npm update.
   1. Then open terminal inside server dir and run npm run dev.
